import { WebhookClient } from 'dialogflow-fulfillment';

export const getCourseFromScheduleDay = function getCourseFromScheduleDayHandler(agent: WebhookClient) {
    agent.add(`getCourseFromScheduleDay 🔥`);
};