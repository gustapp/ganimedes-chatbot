import { WebhookClient } from 'dialogflow-fulfillment';

export const getCourseFromScheduleDayHour = function getCourseFromScheduleDayHourHandler(agent: WebhookClient) {
    agent.add(`getCourseFromScheduleDayHour 🔥`);
};