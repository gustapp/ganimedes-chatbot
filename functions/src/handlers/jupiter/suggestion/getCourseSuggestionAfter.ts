import { WebhookClient } from 'dialogflow-fulfillment';

export const getCourseSuggestionAfter = function getCourseSuggestionAfterHandler(agent: WebhookClient) {
    agent.add(`getCourseSuggestionAfter 🔥`);
};