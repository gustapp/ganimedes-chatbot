import { WebhookClient } from 'dialogflow-fulfillment';

export const getCourseSuggestionBefore = function getCourseSuggestionBeforeHandler(agent: WebhookClient) {
    agent.add(`getCourseSuggestionBefore 🔥`);
};