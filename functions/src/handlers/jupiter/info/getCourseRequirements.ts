import { WebhookClient } from 'dialogflow-fulfillment';

export const getCourseRequirements = function getCourseRequirementsHandler(agent: WebhookClient) {
    agent.add(`getCourseRequirements 🔥`);
};